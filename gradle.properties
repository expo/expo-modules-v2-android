org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true

# The compiler-plugin test framework and the native (CMake) build tasks are not
# yet configuration-cache compatible, so it is disabled for now.
org.gradle.configuration-cache=false
